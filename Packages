Package: zett
Version: 0.10.0
Architecture: arm
Maintainer: Alex Sanders Soto
Filename: ./zett_0.10.0_arm.deb
Size: 6266244
MD5sum: fa961795403171545cf40e34ef83d0b8
SHA1: b2ac9a0cb6b68f98beb64db0f6509921c1126b94
SHA256: daec097c05f959e370bb1da16cf6d3db76b1a49c06c1bbb10762175f2beada80
Description: TITAN compiler: HTTPS, crypto, Android, TUI, images, QR, system, audio, NoSQL, web server, charts
 Zett installs the TITAN Language Compiler, examples and documentation.
 Includes a modern standard library with real (non-simulated) modules:
 regex, uuid, hash (SHA/BLAKE3/HMAC), random, datetime, url, dirs,
 compress (gzip/zstd), archive (tar/zip), yaml, xml, blocking HTTPS
 client, DNS resolver, SMTP email, ChaCha20-Poly1305 & AES-GCM AEAD,
 Argon2id/bcrypt password hashing, JWT signing/verification, direct
 access to Termux:API for Android hardware/OS integration (battery,
 GPS, sensors, camera, SMS, clipboard, vibrate, notify, TTS),
 terminal/TUI helpers (colors, cursor, raw mode, keyboard events,
 readline with history, animated progress bars and spinners), image
 processing (PNG/JPEG/WebP/BMP/GIF: load, save, encode, resize, crop,
 rotate, grayscale, blur, brighten), QR code generation
 (ASCII/Unicode/SVG/PNG at every error-correction level), system
 tools (procfs CPU/memory/processes/disks/networks, file-system watcher
 via inotify, Unix signal handling), and audio (WAV I/O and synthesis
 via hound: sine, square, sawtooth, white noise, fades; playback and
 recording through termux-media-player and termux-microphone-record), and
 NoSQL storage (embedded ACID key-value database via sled with named
 sub-buckets and compare-and-swap, plus a blocking Redis client with
 strings, lists, hashes, TTL and raw commands), and a pure-Rust HTTP/1.1
 web server (tiny_http) with a radix-tree URL router (matchit, the same
 router axum uses under the hood) supporting named and catch-all path
 parameters, JSON/HTML/bytes responses and RFC 6455 WebSocket upgrades,
 and pure-Rust SVG charts via plotters (line, multi-line, bar, scatter
 and histogram outputs to standalone .svg files that any viewer can
 render without shipping a font).

Package: zett
Version: 0.11.0
Architecture: arm
Maintainer: Alex Sanders Soto
Filename: ./zett_0.11.0_arm.deb
Size: 6840860
MD5sum: abc89906617343d46cb1c60d04d3ecfa
SHA1: 8dabf001c8c15a30cdadd58aba42eae994b20775
SHA256: 7a5aa29ca2439869acee227ecad66a82ded163ef262c4867be4b5b8e436ca6f4
Description: TITAN compiler: HTTPS, crypto, Android, TUI, images, QR, system, audio, NoSQL, web server, charts, HF tokenizers
 Zett installs the TITAN Language Compiler, examples and documentation.
 Includes a modern standard library with real (non-simulated) modules:
 regex, uuid, hash (SHA/BLAKE3/HMAC), random, datetime, url, dirs,
 compress (gzip/zstd), archive (tar/zip), yaml, xml, blocking HTTPS
 client, DNS resolver, SMTP email, ChaCha20-Poly1305 & AES-GCM AEAD,
 Argon2id/bcrypt password hashing, JWT signing/verification, direct
 access to Termux:API for Android hardware/OS integration (battery,
 GPS, sensors, camera, SMS, clipboard, vibrate, notify, TTS),
 terminal/TUI helpers (colors, cursor, raw mode, keyboard events,
 readline with history, animated progress bars and spinners), image
 processing (PNG/JPEG/WebP/BMP/GIF: load, save, encode, resize, crop,
 rotate, grayscale, blur, brighten), QR code generation
 (ASCII/Unicode/SVG/PNG at every error-correction level), system
 tools (procfs CPU/memory/processes/disks/networks, file-system watcher
 via inotify, Unix signal handling), and audio (WAV I/O and synthesis
 via hound: sine, square, sawtooth, white noise, fades; playback and
 recording through termux-media-player and termux-microphone-record), and
 NoSQL storage (embedded ACID key-value database via sled with named
 sub-buckets and compare-and-swap, plus a blocking Redis client with
 strings, lists, hashes, TTL and raw commands), and a pure-Rust HTTP/1.1
 web server (tiny_http) with a radix-tree URL router (matchit, the same
 router axum uses under the hood) supporting named and catch-all path
 parameters, JSON/HTML/bytes responses and RFC 6455 WebSocket upgrades,
 and pure-Rust SVG charts via plotters (line, multi-line, bar, scatter
 and histogram outputs to standalone .svg files that any viewer can
 render without shipping a font), and HuggingFace text tokenizers
 (BPE / WordPiece / Unigram) via the official `tokenizers` crate in a
 pure-Rust configuration — reads any HF `tokenizer.json` and exposes
 encode / encode_batch / decode / vocab_size / token_to_id lookups.

Package: zett
Version: 0.12.0
Architecture: arm
Maintainer: Alex Sanders Soto
Filename: ./zett_0.12.0_arm.deb
Size: 9546756
MD5sum: d454f49211fa03ec1b79d7e6d89022bf
SHA1: a737bde62dede857a6e55b09f58a1a0d3e87fdbf
SHA256: c63290649054029ae6fe1fd457bbd905b71e6397233e31537f8fa829cdcc6f8b
Description: TITAN compiler: HTTPS, crypto, Android, TUI, images, QR, system, audio, NoSQL, web server, charts, HF tokenizers, ONNX inference
 Zett installs the TITAN Language Compiler, examples and documentation.
 Includes a modern standard library with real (non-simulated) modules:
 regex, uuid, hash (SHA/BLAKE3/HMAC), random, datetime, url, dirs,
 compress (gzip/zstd), archive (tar/zip), yaml, xml, blocking HTTPS
 client, DNS resolver, SMTP email, ChaCha20-Poly1305 & AES-GCM AEAD,
 Argon2id/bcrypt password hashing, JWT signing/verification, direct
 access to Termux:API for Android hardware/OS integration (battery,
 GPS, sensors, camera, SMS, clipboard, vibrate, notify, TTS),
 terminal/TUI helpers (colors, cursor, raw mode, keyboard events,
 readline with history, animated progress bars and spinners), image
 processing (PNG/JPEG/WebP/BMP/GIF: load, save, encode, resize, crop,
 rotate, grayscale, blur, brighten), QR code generation
 (ASCII/Unicode/SVG/PNG at every error-correction level), system
 tools (procfs CPU/memory/processes/disks/networks, file-system watcher
 via inotify, Unix signal handling), and audio (WAV I/O and synthesis
 via hound: sine, square, sawtooth, white noise, fades; playback and
 recording through termux-media-player and termux-microphone-record), and
 NoSQL storage (embedded ACID key-value database via sled with named
 sub-buckets and compare-and-swap, plus a blocking Redis client with
 strings, lists, hashes, TTL and raw commands), and a pure-Rust HTTP/1.1
 web server (tiny_http) with a radix-tree URL router (matchit, the same
 router axum uses under the hood) supporting named and catch-all path
 parameters, JSON/HTML/bytes responses and RFC 6455 WebSocket upgrades,
 and pure-Rust SVG charts via plotters (line, multi-line, bar, scatter
 and histogram outputs to standalone .svg files that any viewer can
 render without shipping a font), and HuggingFace text tokenizers
 (BPE / WordPiece / Unigram) via the official `tokenizers` crate in a
 pure-Rust configuration — reads any HF `tokenizer.json` and exposes
 encode / encode_batch / decode / vocab_size / token_to_id lookups,
 and on-device ONNX inference via `tract-onnx` (Sonos' production
 inference engine, 100% Rust, no CUDA / cuDNN / BLAS / ONNX Runtime
 C++), able to load .onnx models, inspect their input/output shapes
 and run f32 or i64 tensor inputs entirely on the phone's CPU.

Package: zett
Version: 0.13.0
Architecture: arm
Maintainer: Alex Sanders Soto
Filename: ./zett_0.13.0_arm.deb
Size: 9549500
MD5sum: 5f9a3a594c5ae883316a16317762f987
SHA1: 34c135cf9abfe4e161d00975edd6eaa8adb53dd8
SHA256: 28847e935508e65f9b75a939b4d0d0dc4dc9604cfe11f6fea7f20c95dd174e3f
Description: TITAN compiler: HTTPS, crypto, Android, TUI, images, QR, system, audio, NoSQL, web server, charts, HF tokenizers, ONNX inference (BERT-family multi-input)
 Zett installs the TITAN Language Compiler, examples and documentation.
 Includes a modern standard library with real (non-simulated) modules:
 regex, uuid, hash (SHA/BLAKE3/HMAC), random, datetime, url, dirs,
 compress (gzip/zstd), archive (tar/zip), yaml, xml, blocking HTTPS
 client, DNS resolver, SMTP email, ChaCha20-Poly1305 & AES-GCM AEAD,
 Argon2id/bcrypt password hashing, JWT signing/verification, direct
 access to Termux:API for Android hardware/OS integration (battery,
 GPS, sensors, camera, SMS, clipboard, vibrate, notify, TTS),
 terminal/TUI helpers (colors, cursor, raw mode, keyboard events,
 readline with history, animated progress bars and spinners), image
 processing (PNG/JPEG/WebP/BMP/GIF: load, save, encode, resize, crop,
 rotate, grayscale, blur, brighten), QR code generation
 (ASCII/Unicode/SVG/PNG at every error-correction level), system
 tools (procfs CPU/memory/processes/disks/networks, file-system watcher
 via inotify, Unix signal handling), and audio (WAV I/O and synthesis
 via hound: sine, square, sawtooth, white noise, fades; playback and
 recording through termux-media-player and termux-microphone-record), and
 NoSQL storage (embedded ACID key-value database via sled with named
 sub-buckets and compare-and-swap, plus a blocking Redis client with
 strings, lists, hashes, TTL and raw commands), and a pure-Rust HTTP/1.1
 web server (tiny_http) with a radix-tree URL router (matchit, the same
 router axum uses under the hood) supporting named and catch-all path
 parameters, JSON/HTML/bytes responses and RFC 6455 WebSocket upgrades,
 and pure-Rust SVG charts via plotters (line, multi-line, bar, scatter
 and histogram outputs to standalone .svg files that any viewer can
 render without shipping a font), and HuggingFace text tokenizers
 (BPE / WordPiece / Unigram) via the official `tokenizers` crate in a
 pure-Rust configuration — reads any HF `tokenizer.json` and exposes
 encode / encode_batch / decode / vocab_size / token_to_id lookups,
 and on-device ONNX inference via `tract-onnx` (Sonos' production
 inference engine, 100% Rust, no CUDA / cuDNN / BLAS / ONNX Runtime
 C++), able to load .onnx models, inspect their input/output shapes
 and run f32 or i64 tensor inputs entirely on the phone's CPU, plus
 multi-input BERT-family loaders (input_ids + attention_mask, with
 or without token_type_ids) so DistilBERT / MiniLM / classic BERT
 classifiers and embedding models can be tokenized with std::tokenize
 and forwarded through std::onnx in a single pipeline on the device.

Package: zett
Version: 0.14.0
Architecture: arm
Maintainer: Alex Sanders Soto
Filename: ./zett_0.14.0_arm.deb
Size: 9552756
MD5sum: 2856700ef31a58a49fae6957579b028d
SHA1: 2b7ccc2c91533e3558285ed98cb5eaaa282ee784
SHA256: 5deae926a0bc3f9f6877aeb9e1ff7898cd36122ace2d54939958ccdb3b1a69f3
Description: TITAN compiler: HTTPS, crypto, Android, TUI, images, QR, system, audio, NoSQL, web server, charts, HF tokenizers, ONNX inference (BERT-family multi-input), Wi-Fi scanning
 Zett installs the TITAN Language Compiler, examples and documentation.
 Includes a modern standard library with real (non-simulated) modules:
 regex, uuid, hash (SHA/BLAKE3/HMAC), random, datetime, url, dirs,
 compress (gzip/zstd), archive (tar/zip), yaml, xml, blocking HTTPS
 client, DNS resolver, SMTP email, ChaCha20-Poly1305 & AES-GCM AEAD,
 Argon2id/bcrypt password hashing, JWT signing/verification, direct
 access to Termux:API for Android hardware/OS integration (battery,
 GPS, sensors, camera, SMS, clipboard, vibrate, notify, TTS),
 terminal/TUI helpers (colors, cursor, raw mode, keyboard events,
 readline with history, animated progress bars and spinners), image
 processing (PNG/JPEG/WebP/BMP/GIF: load, save, encode, resize, crop,
 rotate, grayscale, blur, brighten), QR code generation
 (ASCII/Unicode/SVG/PNG at every error-correction level), system
 tools (procfs CPU/memory/processes/disks/networks, file-system watcher
 via inotify, Unix signal handling), and audio (WAV I/O and synthesis
 via hound: sine, square, sawtooth, white noise, fades; playback and
 recording through termux-media-player and termux-microphone-record), and
 NoSQL storage (embedded ACID key-value database via sled with named
 sub-buckets and compare-and-swap, plus a blocking Redis client with
 strings, lists, hashes, TTL and raw commands), and a pure-Rust HTTP/1.1
 web server (tiny_http) with a radix-tree URL router (matchit, the same
 router axum uses under the hood) supporting named and catch-all path
 parameters, JSON/HTML/bytes responses and RFC 6455 WebSocket upgrades,
 and pure-Rust SVG charts via plotters (line, multi-line, bar, scatter
 and histogram outputs to standalone .svg files that any viewer can
 render without shipping a font), and HuggingFace text tokenizers
 (BPE / WordPiece / Unigram) via the official `tokenizers` crate in a
 pure-Rust configuration — reads any HF `tokenizer.json` and exposes
 encode / encode_batch / decode / vocab_size / token_to_id lookups,
 and on-device ONNX inference via `tract-onnx` (Sonos' production
 inference engine, 100% Rust, no CUDA / cuDNN / BLAS / ONNX Runtime
 C++), able to load .onnx models, inspect their input/output shapes
 and run f32 or i64 tensor inputs entirely on the phone's CPU, plus
 multi-input BERT-family loaders (input_ids + attention_mask, with
 or without token_type_ids) so DistilBERT / MiniLM / classic BERT
 classifiers and embedding models can be tokenized with std::tokenize
 and forwarded through std::onnx in a single pipeline on the device,
 and Wi-Fi introspection (std::wifi::scan / connection_info / set_enabled
 / signal_bars) via the official termux-wifi-* CLIs shipped by the
 Termux:API package.

Package: zett
Version: 0.15.0
Architecture: arm
Maintainer: Alex Sanders Soto
Filename: ./zett_0.15.0_arm.deb
Size: 9571416
MD5sum: ebb6b0c61c98ea6f36d595144ca49a17
SHA1: 8fefe8ae7fe55bded2a3dc99e70552dca1285365
SHA256: f4ef85c5de2cab49bb4871e84eeab273d910052eb8f8b765c2e0c403ec6b0ee3
Description: TITAN compiler: HTTPS, crypto, Android, TUI, images, QR, system, audio, NoSQL, web server, charts, HF tokenizers, ONNX inference (BERT-family multi-input, sentence-transformer pooling), Wi-Fi scanning, vector math (semantic search)
 Zett installs the TITAN Language Compiler, examples and documentation.
 Includes a modern standard library with real (non-simulated) modules:
 regex, uuid, hash (SHA/BLAKE3/HMAC), random, datetime, url, dirs,
 compress (gzip/zstd), archive (tar/zip), yaml, xml, blocking HTTPS
 client, DNS resolver, SMTP email, ChaCha20-Poly1305 & AES-GCM AEAD,
 Argon2id/bcrypt password hashing, JWT signing/verification, direct
 access to Termux:API for Android hardware/OS integration (battery,
 GPS, sensors, camera, SMS, clipboard, vibrate, notify, TTS),
 terminal/TUI helpers (colors, cursor, raw mode, keyboard events,
 readline with history, animated progress bars and spinners), image
 processing (PNG/JPEG/WebP/BMP/GIF: load, save, encode, resize, crop,
 rotate, grayscale, blur, brighten), QR code generation
 (ASCII/Unicode/SVG/PNG at every error-correction level), system
 tools (procfs CPU/memory/processes/disks/networks, file-system watcher
 via inotify, Unix signal handling), and audio (WAV I/O and synthesis
 via hound: sine, square, sawtooth, white noise, fades; playback and
 recording through termux-media-player and termux-microphone-record), and
 NoSQL storage (embedded ACID key-value database via sled with named
 sub-buckets and compare-and-swap, plus a blocking Redis client with
 strings, lists, hashes, TTL and raw commands), and a pure-Rust HTTP/1.1
 web server (tiny_http) with a radix-tree URL router (matchit, the same
 router axum uses under the hood) supporting named and catch-all path
 parameters, JSON/HTML/bytes responses and RFC 6455 WebSocket upgrades,
 and pure-Rust SVG charts via plotters (line, multi-line, bar, scatter
 and histogram outputs to standalone .svg files that any viewer can
 render without shipping a font), and HuggingFace text tokenizers
 (BPE / WordPiece / Unigram) via the official `tokenizers` crate in a
 pure-Rust configuration — reads any HF `tokenizer.json` and exposes
 encode / encode_batch / decode / vocab_size / token_to_id lookups,
 and on-device ONNX inference via `tract-onnx` (Sonos' production
 inference engine, 100% Rust, no CUDA / cuDNN / BLAS / ONNX Runtime
 C++), able to load .onnx models, inspect their input/output shapes
 and run f32 or i64 tensor inputs entirely on the phone's CPU, plus
 multi-input BERT-family loaders (input_ids + attention_mask, with
 or without token_type_ids) so DistilBERT / MiniLM / classic BERT
 classifiers and embedding models can be tokenized with std::tokenize
 and forwarded through std::onnx in a single pipeline on the device,
 and Wi-Fi introspection (std::wifi::scan / connection_info / set_enabled
 / signal_bars) via the official termux-wifi-* CLIs shipped by the
 Termux:API package, plus sentence-transformer style embedding
 pooling (std::onnx::run_bert_pooled — attention-mask-weighted mean
 pool of the encoder's last_hidden_state) and pure-Rust vector math
 (std::vector::dot / norm / cosine_similarity / normalize / add / sub
 / scale / argmax) for on-device semantic search over MiniLM
 embeddings.

Package: zett
Version: 0.16.0
Architecture: arm
Maintainer: Alex Sanders Soto
Filename: ./zett_0.16.0_arm.deb
Size: 9582832
MD5sum: ae90247ab769b05a5bb007e8e29cadd4
SHA1: 0f13d698fe1ead5cd985d7c452de70ec5d1fa780
SHA256: 16682e345fb0abea32b7c79494883c5b0b1a53aa41bd271e5038be1841bdcf56
Description: TITAN compiler: HTTPS, crypto, Android, TUI, images, QR, system, audio, NoSQL, web server, charts, HF tokenizers, ONNX inference (BERT-family multi-input, sentence-transformer pooling), Wi-Fi scanning, vector math (semantic search), PDF generation
 Zett installs the TITAN Language Compiler, examples and documentation.
 Includes a modern standard library with real (non-simulated) modules:
 regex, uuid, hash (SHA/BLAKE3/HMAC), random, datetime, url, dirs,
 compress (gzip/zstd), archive (tar/zip), yaml, xml, blocking HTTPS
 client, DNS resolver, SMTP email, ChaCha20-Poly1305 & AES-GCM AEAD,
 Argon2id/bcrypt password hashing, JWT signing/verification, direct
 access to Termux:API for Android hardware/OS integration (battery,
 GPS, sensors, camera, SMS, clipboard, vibrate, notify, TTS),
 terminal/TUI helpers (colors, cursor, raw mode, keyboard events,
 readline with history, animated progress bars and spinners), image
 processing (PNG/JPEG/WebP/BMP/GIF: load, save, encode, resize, crop,
 rotate, grayscale, blur, brighten), QR code generation
 (ASCII/Unicode/SVG/PNG at every error-correction level), system
 tools (procfs CPU/memory/processes/disks/networks, file-system watcher
 via inotify, Unix signal handling), and audio (WAV I/O and synthesis
 via hound: sine, square, sawtooth, white noise, fades; playback and
 recording through termux-media-player and termux-microphone-record), and
 NoSQL storage (embedded ACID key-value database via sled with named
 sub-buckets and compare-and-swap, plus a blocking Redis client with
 strings, lists, hashes, TTL and raw commands), and a pure-Rust HTTP/1.1
 web server (tiny_http) with a radix-tree URL router (matchit, the same
 router axum uses under the hood) supporting named and catch-all path
 parameters, JSON/HTML/bytes responses and RFC 6455 WebSocket upgrades,
 and pure-Rust SVG charts via plotters (line, multi-line, bar, scatter
 and histogram outputs to standalone .svg files that any viewer can
 render without shipping a font), and HuggingFace text tokenizers
 (BPE / WordPiece / Unigram) via the official `tokenizers` crate in a
 pure-Rust configuration — reads any HF `tokenizer.json` and exposes
 encode / encode_batch / decode / vocab_size / token_to_id lookups,
 and on-device ONNX inference via `tract-onnx` (Sonos' production
 inference engine, 100% Rust, no CUDA / cuDNN / BLAS / ONNX Runtime
 C++), able to load .onnx models, inspect their input/output shapes
 and run f32 or i64 tensor inputs entirely on the phone's CPU, plus
 multi-input BERT-family loaders (input_ids + attention_mask, with
 or without token_type_ids) so DistilBERT / MiniLM / classic BERT
 classifiers and embedding models can be tokenized with std::tokenize
 and forwarded through std::onnx in a single pipeline on the device,
 and Wi-Fi introspection (std::wifi::scan / connection_info / set_enabled
 / signal_bars) via the official termux-wifi-* CLIs shipped by the
 Termux:API package, plus sentence-transformer style embedding
 pooling (std::onnx::run_bert_pooled — attention-mask-weighted mean
 pool of the encoder's last_hidden_state) and pure-Rust vector math
 (std::vector::dot / norm / cosine_similarity / normalize / add / sub
 / scale / argmax) for on-device semantic search over MiniLM
 embeddings, and PDF generation (std::pdf::new / add_page / add_text /
 set_color / add_line / add_rect / save) built on printpdf 0.7 in a
 no-defaults pure-Rust config (no azul-layout, no rust-fontconfig, no
 HTML rendering — just the core PDF writer with the 14 built-in fonts).

Package: zett
Version: 0.17.0
Architecture: arm
Maintainer: Alex Sanders Soto
Filename: ./zett_0.17.0_arm.deb
Size: 9587908
MD5sum: 57aa032257b30064a3618fa003afaa8f
SHA1: 93a8e496516fe32ad352be18c663fd341ebce4f8
SHA256: 052f7d8688ad60b316302511d493cdcef87389aada6a61c1c3382bba5cae9d48
Description: TITAN compiler: HTTPS, crypto, Android, TUI, images, QR, system, audio, NoSQL, web server, charts, HF tokenizers, ONNX inference (BERT-family multi-input, sentence-transformer pooling), Wi-Fi scanning, vector math (semantic search), PDF generation
 Zett installs the TITAN Language Compiler, examples and documentation.
 Includes a modern standard library with real (non-simulated) modules:
 regex, uuid, hash (SHA/BLAKE3/HMAC), random, datetime, url, dirs,
 compress (gzip/zstd), archive (tar/zip), yaml, xml, blocking HTTPS
 client, DNS resolver, SMTP email, ChaCha20-Poly1305 & AES-GCM AEAD,
 Argon2id/bcrypt password hashing, JWT signing/verification, direct
 access to Termux:API for Android hardware/OS integration (battery,
 GPS, sensors, camera, SMS, clipboard, vibrate, notify, TTS),
 terminal/TUI helpers (colors, cursor, raw mode, keyboard events,
 readline with history, animated progress bars and spinners), image
 processing (PNG/JPEG/WebP/BMP/GIF: load, save, encode, resize, crop,
 rotate, grayscale, blur, brighten), QR code generation
 (ASCII/Unicode/SVG/PNG at every error-correction level), system
 tools (procfs CPU/memory/processes/disks/networks, file-system watcher
 via inotify, Unix signal handling), and audio (WAV I/O and synthesis
 via hound: sine, square, sawtooth, white noise, fades; playback and
 recording through termux-media-player and termux-microphone-record), and
 NoSQL storage (embedded ACID key-value database via sled with named
 sub-buckets and compare-and-swap, plus a blocking Redis client with
 strings, lists, hashes, TTL and raw commands), and a pure-Rust HTTP/1.1
 web server (tiny_http) with a radix-tree URL router (matchit, the same
 router axum uses under the hood) supporting named and catch-all path
 parameters, JSON/HTML/bytes responses and RFC 6455 WebSocket upgrades,
 and pure-Rust SVG charts via plotters (line, multi-line, bar, scatter
 and histogram outputs to standalone .svg files that any viewer can
 render without shipping a font), and HuggingFace text tokenizers
 (BPE / WordPiece / Unigram) via the official `tokenizers` crate in a
 pure-Rust configuration — reads any HF `tokenizer.json` and exposes
 encode / encode_batch / decode / vocab_size / token_to_id lookups,
 and on-device ONNX inference via `tract-onnx` (Sonos' production
 inference engine, 100% Rust, no CUDA / cuDNN / BLAS / ONNX Runtime
 C++), able to load .onnx models, inspect their input/output shapes
 and run f32 or i64 tensor inputs entirely on the phone's CPU, plus
 multi-input BERT-family loaders (input_ids + attention_mask, with
 or without token_type_ids) so DistilBERT / MiniLM / classic BERT
 classifiers and embedding models can be tokenized with std::tokenize
 and forwarded through std::onnx in a single pipeline on the device,
 and Wi-Fi introspection (std::wifi::scan / connection_info / set_enabled
 / signal_bars) via the official termux-wifi-* CLIs shipped by the
 Termux:API package, plus sentence-transformer style embedding
 pooling (std::onnx::run_bert_pooled — attention-mask-weighted mean
 pool of the encoder's last_hidden_state) and pure-Rust vector math
 (std::vector::dot / norm / cosine_similarity / normalize / add / sub
 / scale / argmax) for on-device semantic search over MiniLM
 embeddings, and PDF generation (std::pdf::new / add_page / add_text /
 set_color / add_line / add_rect / save) built on printpdf 0.7 in a
 no-defaults pure-Rust config (no azul-layout, no rust-fontconfig, no
 HTML rendering — just the core PDF writer with the 14 built-in fonts).

Package: zett
Version: 0.18.0
Architecture: arm
Maintainer: Alex Sanders Soto
Filename: ./zett_0.18.0_arm.deb
Size: 9594036
MD5sum: 1a2d02c6346bd002cd5214fc06a38127
SHA1: 2477e0f5aef830b3c90008cb81cbb620dca868a6
SHA256: 9ba3ee9e0bbad9b13815f33869ef1fdc9264e7a12f944aed57b9222fb7996d93
Description: TITAN compiler: HTTPS, crypto, Android, TUI, images, QR, system, audio, NoSQL, web server, charts, HF tokenizers, ONNX inference (BERT-family multi-input, sentence-transformer pooling), Wi-Fi scanning, vector math (semantic search), PDF generation
 Zett installs the TITAN Language Compiler, examples and documentation.
 Includes a modern standard library with real (non-simulated) modules:
 regex, uuid, hash (SHA/BLAKE3/HMAC), random, datetime, url, dirs,
 compress (gzip/zstd), archive (tar/zip), yaml, xml, blocking HTTPS
 client, DNS resolver, SMTP email, ChaCha20-Poly1305 & AES-GCM AEAD,
 Argon2id/bcrypt password hashing, JWT signing/verification, direct
 access to Termux:API for Android hardware/OS integration (battery,
 GPS, sensors, camera, SMS, clipboard, vibrate, notify, TTS),
 terminal/TUI helpers (colors, cursor, raw mode, keyboard events,
 readline with history, animated progress bars and spinners), image
 processing (PNG/JPEG/WebP/BMP/GIF: load, save, encode, resize, crop,
 rotate, grayscale, blur, brighten), QR code generation
 (ASCII/Unicode/SVG/PNG at every error-correction level), system
 tools (procfs CPU/memory/processes/disks/networks, file-system watcher
 via inotify, Unix signal handling), and audio (WAV I/O and synthesis
 via hound: sine, square, sawtooth, white noise, fades; playback and
 recording through termux-media-player and termux-microphone-record), and
 NoSQL storage (embedded ACID key-value database via sled with named
 sub-buckets and compare-and-swap, plus a blocking Redis client with
 strings, lists, hashes, TTL and raw commands), and a pure-Rust HTTP/1.1
 web server (tiny_http) with a radix-tree URL router (matchit, the same
 router axum uses under the hood) supporting named and catch-all path
 parameters, JSON/HTML/bytes responses and RFC 6455 WebSocket upgrades,
 and pure-Rust SVG charts via plotters (line, multi-line, bar, scatter
 and histogram outputs to standalone .svg files that any viewer can
 render without shipping a font), and HuggingFace text tokenizers
 (BPE / WordPiece / Unigram) via the official `tokenizers` crate in a
 pure-Rust configuration — reads any HF `tokenizer.json` and exposes
 encode / encode_batch / decode / vocab_size / token_to_id lookups,
 and on-device ONNX inference via `tract-onnx` (Sonos' production
 inference engine, 100% Rust, no CUDA / cuDNN / BLAS / ONNX Runtime
 C++), able to load .onnx models, inspect their input/output shapes
 and run f32 or i64 tensor inputs entirely on the phone's CPU, plus
 multi-input BERT-family loaders (input_ids + attention_mask, with
 or without token_type_ids) so DistilBERT / MiniLM / classic BERT
 classifiers and embedding models can be tokenized with std::tokenize
 and forwarded through std::onnx in a single pipeline on the device,
 and Wi-Fi introspection (std::wifi::scan / connection_info / set_enabled
 / signal_bars) via the official termux-wifi-* CLIs shipped by the
 Termux:API package, plus sentence-transformer style embedding
 pooling (std::onnx::run_bert_pooled — attention-mask-weighted mean
 pool of the encoder's last_hidden_state) and pure-Rust vector math
 (std::vector::dot / norm / cosine_similarity / normalize / add / sub
 / scale / argmax) for on-device semantic search over MiniLM
 embeddings, and PDF generation (std::pdf::new / add_page / add_text /
 set_color / add_line / add_rect / save) built on printpdf 0.7 in a
 no-defaults pure-Rust config (no azul-layout, no rust-fontconfig, no
 HTML rendering — just the core PDF writer with the 14 built-in fonts).

Package: zett
Version: 0.19.0
Architecture: arm
Maintainer: Alex Sanders Soto
Filename: ./zett_0.19.0_arm.deb
Size: 9606912
MD5sum: 664af294f0909dd49b1f474e31cc0c64
SHA1: 7f28ce21adee704a6675caba8f9b076453d79e15
SHA256: a338a13dc5f6860d8c1e02f8e388a19ce6205478cc436fd206e35df24a46f725
Description: TITAN compiler: HTTPS, crypto, Android, TUI, images, QR, system, audio, NoSQL, web server, charts, HF tokenizers, ONNX inference (BERT-family multi-input, sentence-transformer pooling), Wi-Fi scanning, vector math (semantic search), PDF generation
 Zett installs the TITAN Language Compiler, examples and documentation.
 Includes a modern standard library with real (non-simulated) modules:
 regex, uuid, hash (SHA/BLAKE3/HMAC), random, datetime, url, dirs,
 compress (gzip/zstd), archive (tar/zip), yaml, xml, blocking HTTPS
 client, DNS resolver, SMTP email, ChaCha20-Poly1305 & AES-GCM AEAD,
 Argon2id/bcrypt password hashing, JWT signing/verification, direct
 access to Termux:API for Android hardware/OS integration (battery,
 GPS, sensors, camera, SMS, clipboard, vibrate, notify, TTS),
 terminal/TUI helpers (colors, cursor, raw mode, keyboard events,
 readline with history, animated progress bars and spinners), image
 processing (PNG/JPEG/WebP/BMP/GIF: load, save, encode, resize, crop,
 rotate, grayscale, blur, brighten), QR code generation
 (ASCII/Unicode/SVG/PNG at every error-correction level), system
 tools (procfs CPU/memory/processes/disks/networks, file-system watcher
 via inotify, Unix signal handling), and audio (WAV I/O and synthesis
 via hound: sine, square, sawtooth, white noise, fades; playback and
 recording through termux-media-player and termux-microphone-record), and
 NoSQL storage (embedded ACID key-value database via sled with named
 sub-buckets and compare-and-swap, plus a blocking Redis client with
 strings, lists, hashes, TTL and raw commands), and a pure-Rust HTTP/1.1
 web server (tiny_http) with a radix-tree URL router (matchit, the same
 router axum uses under the hood) supporting named and catch-all path
 parameters, JSON/HTML/bytes responses and RFC 6455 WebSocket upgrades,
 and pure-Rust SVG charts via plotters (line, multi-line, bar, scatter
 and histogram outputs to standalone .svg files that any viewer can
 render without shipping a font), and HuggingFace text tokenizers
 (BPE / WordPiece / Unigram) via the official `tokenizers` crate in a
 pure-Rust configuration — reads any HF `tokenizer.json` and exposes
 encode / encode_batch / decode / vocab_size / token_to_id lookups,
 and on-device ONNX inference via `tract-onnx` (Sonos' production
 inference engine, 100% Rust, no CUDA / cuDNN / BLAS / ONNX Runtime
 C++), able to load .onnx models, inspect their input/output shapes
 and run f32 or i64 tensor inputs entirely on the phone's CPU, plus
 multi-input BERT-family loaders (input_ids + attention_mask, with
 or without token_type_ids) so DistilBERT / MiniLM / classic BERT
 classifiers and embedding models can be tokenized with std::tokenize
 and forwarded through std::onnx in a single pipeline on the device,
 and Wi-Fi introspection (std::wifi::scan / connection_info / set_enabled
 / signal_bars) via the official termux-wifi-* CLIs shipped by the
 Termux:API package, plus sentence-transformer style embedding
 pooling (std::onnx::run_bert_pooled — attention-mask-weighted mean
 pool of the encoder's last_hidden_state) and pure-Rust vector math
 (std::vector::dot / norm / cosine_similarity / normalize / add / sub
 / scale / argmax) for on-device semantic search over MiniLM
 embeddings, and PDF generation (std::pdf::new / add_page / add_text /
 set_color / add_line / add_rect / save) built on printpdf 0.7 in a
 no-defaults pure-Rust config (no azul-layout, no rust-fontconfig, no
 HTML rendering — just the core PDF writer with the 14 built-in fonts).

Package: zett
Version: 0.2.0
Architecture: arm
Maintainer: Alex Sanders Soto
Filename: ./zett_0.2.0_arm.deb
Size: 4989728
MD5sum: 35b30b0ad4da40c2c651de0c9c2b4b24
SHA1: a22ba53822c9b9d59cac2c2d7eeaad4817f2a8f9
SHA256: d64ff7e56861d231e0f780362957f8dc34af25fce4b510ad5ce781b2134ae420
Description: Zett package containing the TITAN language compiler and ecosystem
 Zett installs the TITAN Language Compiler, examples, documentation,
 and the complete TITAN source ecosystem.

Package: zett
Version: 0.20.0
Architecture: arm
Maintainer: Alex Sanders Soto
Filename: ./zett_0.20.0_arm.deb
Size: 9607808
MD5sum: 4bb763ffe51c1e6ec6aeef90590f9aca
SHA1: d80ea2a245aedefe83791b9f6d6cdab18c74ac94
SHA256: d2bb6dcd4a6dc53ed116296397bc89d909733ee130459227c1e43c2bcd61cd29
Description: TITAN compiler: HTTPS, crypto, Android, TUI, images, QR, system, audio, NoSQL, web server, charts, HF tokenizers, ONNX inference (BERT-family multi-input, sentence-transformer pooling), Wi-Fi scanning, vector math (semantic search), PDF generation
 Zett installs the TITAN Language Compiler, examples and documentation.
 Includes a modern standard library with real (non-simulated) modules:
 regex, uuid, hash (SHA/BLAKE3/HMAC), random, datetime, url, dirs,
 compress (gzip/zstd), archive (tar/zip), yaml, xml, blocking HTTPS
 client, DNS resolver, SMTP email, ChaCha20-Poly1305 & AES-GCM AEAD,
 Argon2id/bcrypt password hashing, JWT signing/verification, direct
 access to Termux:API for Android hardware/OS integration (battery,
 GPS, sensors, camera, SMS, clipboard, vibrate, notify, TTS),
 terminal/TUI helpers (colors, cursor, raw mode, keyboard events,
 readline with history, animated progress bars and spinners), image
 processing (PNG/JPEG/WebP/BMP/GIF: load, save, encode, resize, crop,
 rotate, grayscale, blur, brighten), QR code generation
 (ASCII/Unicode/SVG/PNG at every error-correction level), system
 tools (procfs CPU/memory/processes/disks/networks, file-system watcher
 via inotify, Unix signal handling), and audio (WAV I/O and synthesis
 via hound: sine, square, sawtooth, white noise, fades; playback and
 recording through termux-media-player and termux-microphone-record), and
 NoSQL storage (embedded ACID key-value database via sled with named
 sub-buckets and compare-and-swap, plus a blocking Redis client with
 strings, lists, hashes, TTL and raw commands), and a pure-Rust HTTP/1.1
 web server (tiny_http) with a radix-tree URL router (matchit, the same
 router axum uses under the hood) supporting named and catch-all path
 parameters, JSON/HTML/bytes responses and RFC 6455 WebSocket upgrades,
 and pure-Rust SVG charts via plotters (line, multi-line, bar, scatter
 and histogram outputs to standalone .svg files that any viewer can
 render without shipping a font), and HuggingFace text tokenizers
 (BPE / WordPiece / Unigram) via the official `tokenizers` crate in a
 pure-Rust configuration — reads any HF `tokenizer.json` and exposes
 encode / encode_batch / decode / vocab_size / token_to_id lookups,
 and on-device ONNX inference via `tract-onnx` (Sonos' production
 inference engine, 100% Rust, no CUDA / cuDNN / BLAS / ONNX Runtime
 C++), able to load .onnx models, inspect their input/output shapes
 and run f32 or i64 tensor inputs entirely on the phone's CPU, plus
 multi-input BERT-family loaders (input_ids + attention_mask, with
 or without token_type_ids) so DistilBERT / MiniLM / classic BERT
 classifiers and embedding models can be tokenized with std::tokenize
 and forwarded through std::onnx in a single pipeline on the device,
 and Wi-Fi introspection (std::wifi::scan / connection_info / set_enabled
 / signal_bars) via the official termux-wifi-* CLIs shipped by the
 Termux:API package, plus sentence-transformer style embedding
 pooling (std::onnx::run_bert_pooled — attention-mask-weighted mean
 pool of the encoder's last_hidden_state) and pure-Rust vector math
 (std::vector::dot / norm / cosine_similarity / normalize / add / sub
 / scale / argmax) for on-device semantic search over MiniLM
 embeddings, and PDF generation (std::pdf::new / add_page / add_text /
 set_color / add_line / add_rect / save) built on printpdf 0.7 in a
 no-defaults pure-Rust config (no azul-layout, no rust-fontconfig, no
 HTML rendering — just the core PDF writer with the 14 built-in fonts).

Package: zett
Version: 0.21.0
Architecture: arm
Maintainer: Alex Sanders Soto
Filename: ./zett_0.21.0_arm.deb
Size: 9619908
MD5sum: 169882e568f9b6acf00947a300a47845
SHA1: dfa9e725fc8a8ab36346a85a4ede1e3cd6d545f2
SHA256: c4bd6f50e96ef03bec8f8a544e4665f31ed64b5d818832d6229a797fe9edf7e1
Description: TITAN compiler: HTTPS, crypto, Android, TUI, images, QR, system, audio, NoSQL, web server, charts, HF tokenizers, ONNX inference (BERT-family multi-input, sentence-transformer pooling), Wi-Fi scanning, vector math (semantic search), PDF generation
 Zett installs the TITAN Language Compiler, examples and documentation.
 Includes a modern standard library with real (non-simulated) modules:
 regex, uuid, hash (SHA/BLAKE3/HMAC), random, datetime, url, dirs,
 compress (gzip/zstd), archive (tar/zip), yaml, xml, blocking HTTPS
 client, DNS resolver, SMTP email, ChaCha20-Poly1305 & AES-GCM AEAD,
 Argon2id/bcrypt password hashing, JWT signing/verification, direct
 access to Termux:API for Android hardware/OS integration (battery,
 GPS, sensors, camera, SMS, clipboard, vibrate, notify, TTS),
 terminal/TUI helpers (colors, cursor, raw mode, keyboard events,
 readline with history, animated progress bars and spinners), image
 processing (PNG/JPEG/WebP/BMP/GIF: load, save, encode, resize, crop,
 rotate, grayscale, blur, brighten), QR code generation
 (ASCII/Unicode/SVG/PNG at every error-correction level), system
 tools (procfs CPU/memory/processes/disks/networks, file-system watcher
 via inotify, Unix signal handling), and audio (WAV I/O and synthesis
 via hound: sine, square, sawtooth, white noise, fades; playback and
 recording through termux-media-player and termux-microphone-record), and
 NoSQL storage (embedded ACID key-value database via sled with named
 sub-buckets and compare-and-swap, plus a blocking Redis client with
 strings, lists, hashes, TTL and raw commands), and a pure-Rust HTTP/1.1
 web server (tiny_http) with a radix-tree URL router (matchit, the same
 router axum uses under the hood) supporting named and catch-all path
 parameters, JSON/HTML/bytes responses and RFC 6455 WebSocket upgrades,
 and pure-Rust SVG charts via plotters (line, multi-line, bar, scatter
 and histogram outputs to standalone .svg files that any viewer can
 render without shipping a font), and HuggingFace text tokenizers
 (BPE / WordPiece / Unigram) via the official `tokenizers` crate in a
 pure-Rust configuration — reads any HF `tokenizer.json` and exposes
 encode / encode_batch / decode / vocab_size / token_to_id lookups,
 and on-device ONNX inference via `tract-onnx` (Sonos' production
 inference engine, 100% Rust, no CUDA / cuDNN / BLAS / ONNX Runtime
 C++), able to load .onnx models, inspect their input/output shapes
 and run f32 or i64 tensor inputs entirely on the phone's CPU, plus
 multi-input BERT-family loaders (input_ids + attention_mask, with
 or without token_type_ids) so DistilBERT / MiniLM / classic BERT
 classifiers and embedding models can be tokenized with std::tokenize
 and forwarded through std::onnx in a single pipeline on the device,
 and Wi-Fi introspection (std::wifi::scan / connection_info / set_enabled
 / signal_bars) via the official termux-wifi-* CLIs shipped by the
 Termux:API package, plus sentence-transformer style embedding
 pooling (std::onnx::run_bert_pooled — attention-mask-weighted mean
 pool of the encoder's last_hidden_state) and pure-Rust vector math
 (std::vector::dot / norm / cosine_similarity / normalize / add / sub
 / scale / argmax) for on-device semantic search over MiniLM
 embeddings, and PDF generation (std::pdf::new / add_page / add_text /
 set_color / add_line / add_rect / save) built on printpdf 0.7 in a
 no-defaults pure-Rust config (no azul-layout, no rust-fontconfig, no
 HTML rendering — just the core PDF writer with the 14 built-in fonts).

Package: zett
Version: 0.22.0
Architecture: arm
Maintainer: Alex Sanders Soto
Filename: ./zett_0.22.0_arm.deb
Size: 9624408
MD5sum: 0abfcd6b0960ff7a22a6d52a480dc016
SHA1: 25f1e861f516128cdf661625a3a4b10ff940f32f
SHA256: 274843050c071e0871c54de786c18ef52bc78a02e56f69b3bbc449a54af24911
Description: TITAN compiler: HTTPS, crypto, Android, TUI, images, QR, system, audio, NoSQL, web server, charts, HF tokenizers, ONNX inference (BERT-family multi-input, sentence-transformer pooling), Wi-Fi scanning, vector math (semantic search), PDF generation
 Zett installs the TITAN Language Compiler, examples and documentation.
 Includes a modern standard library with real (non-simulated) modules:
 regex, uuid, hash (SHA/BLAKE3/HMAC), random, datetime, url, dirs,
 compress (gzip/zstd), archive (tar/zip), yaml, xml, blocking HTTPS
 client, DNS resolver, SMTP email, ChaCha20-Poly1305 & AES-GCM AEAD,
 Argon2id/bcrypt password hashing, JWT signing/verification, direct
 access to Termux:API for Android hardware/OS integration (battery,
 GPS, sensors, camera, SMS, clipboard, vibrate, notify, TTS),
 terminal/TUI helpers (colors, cursor, raw mode, keyboard events,
 readline with history, animated progress bars and spinners), image
 processing (PNG/JPEG/WebP/BMP/GIF: load, save, encode, resize, crop,
 rotate, grayscale, blur, brighten), QR code generation
 (ASCII/Unicode/SVG/PNG at every error-correction level), system
 tools (procfs CPU/memory/processes/disks/networks, file-system watcher
 via inotify, Unix signal handling), and audio (WAV I/O and synthesis
 via hound: sine, square, sawtooth, white noise, fades; playback and
 recording through termux-media-player and termux-microphone-record), and
 NoSQL storage (embedded ACID key-value database via sled with named
 sub-buckets and compare-and-swap, plus a blocking Redis client with
 strings, lists, hashes, TTL and raw commands), and a pure-Rust HTTP/1.1
 web server (tiny_http) with a radix-tree URL router (matchit, the same
 router axum uses under the hood) supporting named and catch-all path
 parameters, JSON/HTML/bytes responses and RFC 6455 WebSocket upgrades,
 and pure-Rust SVG charts via plotters (line, multi-line, bar, scatter
 and histogram outputs to standalone .svg files that any viewer can
 render without shipping a font), and HuggingFace text tokenizers
 (BPE / WordPiece / Unigram) via the official `tokenizers` crate in a
 pure-Rust configuration — reads any HF `tokenizer.json` and exposes
 encode / encode_batch / decode / vocab_size / token_to_id lookups,
 and on-device ONNX inference via `tract-onnx` (Sonos' production
 inference engine, 100% Rust, no CUDA / cuDNN / BLAS / ONNX Runtime
 C++), able to load .onnx models, inspect their input/output shapes
 and run f32 or i64 tensor inputs entirely on the phone's CPU, plus
 multi-input BERT-family loaders (input_ids + attention_mask, with
 or without token_type_ids) so DistilBERT / MiniLM / classic BERT
 classifiers and embedding models can be tokenized with std::tokenize
 and forwarded through std::onnx in a single pipeline on the device,
 and Wi-Fi introspection (std::wifi::scan / connection_info / set_enabled
 / signal_bars) via the official termux-wifi-* CLIs shipped by the
 Termux:API package, plus sentence-transformer style embedding
 pooling (std::onnx::run_bert_pooled — attention-mask-weighted mean
 pool of the encoder's last_hidden_state) and pure-Rust vector math
 (std::vector::dot / norm / cosine_similarity / normalize / add / sub
 / scale / argmax) for on-device semantic search over MiniLM
 embeddings, and PDF generation (std::pdf::new / add_page / add_text /
 set_color / add_line / add_rect / save) built on printpdf 0.7 in a
 no-defaults pure-Rust config (no azul-layout, no rust-fontconfig, no
 HTML rendering — just the core PDF writer with the 14 built-in fonts).

Package: zett
Version: 0.23.0
Architecture: arm
Maintainer: Alex Sanders Soto
Filename: ./zett_0.23.0_arm.deb
Size: 9628308
MD5sum: 887587089b955bfee688ae5326e7b162
SHA1: 915b10e58aa608099474b77553047d0049fce0f7
SHA256: 17edc5dc7a03ab152afbbd6b2214fa0b7b5da78904cc9cd0412f7dbf7f15e61a
Description: TITAN compiler: HTTPS, crypto, Android, TUI, images, QR, system, audio, NoSQL, web server, charts, HF tokenizers, ONNX inference (BERT-family multi-input, sentence-transformer pooling), Wi-Fi scanning, vector math (semantic search), PDF generation
 Zett installs the TITAN Language Compiler, examples and documentation.
 Includes a modern standard library with real (non-simulated) modules:
 regex, uuid, hash (SHA/BLAKE3/HMAC), random, datetime, url, dirs,
 compress (gzip/zstd), archive (tar/zip), yaml, xml, blocking HTTPS
 client, DNS resolver, SMTP email, ChaCha20-Poly1305 & AES-GCM AEAD,
 Argon2id/bcrypt password hashing, JWT signing/verification, direct
 access to Termux:API for Android hardware/OS integration (battery,
 GPS, sensors, camera, SMS, clipboard, vibrate, notify, TTS),
 terminal/TUI helpers (colors, cursor, raw mode, keyboard events,
 readline with history, animated progress bars and spinners), image
 processing (PNG/JPEG/WebP/BMP/GIF: load, save, encode, resize, crop,
 rotate, grayscale, blur, brighten), QR code generation
 (ASCII/Unicode/SVG/PNG at every error-correction level), system
 tools (procfs CPU/memory/processes/disks/networks, file-system watcher
 via inotify, Unix signal handling), and audio (WAV I/O and synthesis
 via hound: sine, square, sawtooth, white noise, fades; playback and
 recording through termux-media-player and termux-microphone-record), and
 NoSQL storage (embedded ACID key-value database via sled with named
 sub-buckets and compare-and-swap, plus a blocking Redis client with
 strings, lists, hashes, TTL and raw commands), and a pure-Rust HTTP/1.1
 web server (tiny_http) with a radix-tree URL router (matchit, the same
 router axum uses under the hood) supporting named and catch-all path
 parameters, JSON/HTML/bytes responses and RFC 6455 WebSocket upgrades,
 and pure-Rust SVG charts via plotters (line, multi-line, bar, scatter
 and histogram outputs to standalone .svg files that any viewer can
 render without shipping a font), and HuggingFace text tokenizers
 (BPE / WordPiece / Unigram) via the official `tokenizers` crate in a
 pure-Rust configuration — reads any HF `tokenizer.json` and exposes
 encode / encode_batch / decode / vocab_size / token_to_id lookups,
 and on-device ONNX inference via `tract-onnx` (Sonos' production
 inference engine, 100% Rust, no CUDA / cuDNN / BLAS / ONNX Runtime
 C++), able to load .onnx models, inspect their input/output shapes
 and run f32 or i64 tensor inputs entirely on the phone's CPU, plus
 multi-input BERT-family loaders (input_ids + attention_mask, with
 or without token_type_ids) so DistilBERT / MiniLM / classic BERT
 classifiers and embedding models can be tokenized with std::tokenize
 and forwarded through std::onnx in a single pipeline on the device,
 and Wi-Fi introspection (std::wifi::scan / connection_info / set_enabled
 / signal_bars) via the official termux-wifi-* CLIs shipped by the
 Termux:API package, plus sentence-transformer style embedding
 pooling (std::onnx::run_bert_pooled — attention-mask-weighted mean
 pool of the encoder's last_hidden_state) and pure-Rust vector math
 (std::vector::dot / norm / cosine_similarity / normalize / add / sub
 / scale / argmax) for on-device semantic search over MiniLM
 embeddings, and PDF generation (std::pdf::new / add_page / add_text /
 set_color / add_line / add_rect / save) built on printpdf 0.7 in a
 no-defaults pure-Rust config (no azul-layout, no rust-fontconfig, no
 HTML rendering — just the core PDF writer with the 14 built-in fonts).

Package: zett
Version: 0.24.0
Architecture: arm
Maintainer: Alex Sanders Soto
Filename: ./zett_0.24.0_arm.deb
Size: 9629336
MD5sum: 2334a615775e54b3b3c282c85561ee45
SHA1: e632b9d5063b89839926a58c1540241f3b3582ce
SHA256: b3156f31f443b3f969f5dc5c788757f1e42c62bbfb36e9787ff514dfb341198e
Description: TITAN compiler: HTTPS, crypto, Android, TUI, images, QR, system, audio, NoSQL, web server, charts, HF tokenizers, ONNX inference (BERT-family multi-input, sentence-transformer pooling), Wi-Fi scanning, vector math (semantic search), PDF generation
 Zett installs the TITAN Language Compiler, examples and documentation.
 Includes a modern standard library with real (non-simulated) modules:
 regex, uuid, hash (SHA/BLAKE3/HMAC), random, datetime, url, dirs,
 compress (gzip/zstd), archive (tar/zip), yaml, xml, blocking HTTPS
 client, DNS resolver, SMTP email, ChaCha20-Poly1305 & AES-GCM AEAD,
 Argon2id/bcrypt password hashing, JWT signing/verification, direct
 access to Termux:API for Android hardware/OS integration (battery,
 GPS, sensors, camera, SMS, clipboard, vibrate, notify, TTS),
 terminal/TUI helpers (colors, cursor, raw mode, keyboard events,
 readline with history, animated progress bars and spinners), image
 processing (PNG/JPEG/WebP/BMP/GIF: load, save, encode, resize, crop,
 rotate, grayscale, blur, brighten), QR code generation
 (ASCII/Unicode/SVG/PNG at every error-correction level), system
 tools (procfs CPU/memory/processes/disks/networks, file-system watcher
 via inotify, Unix signal handling), and audio (WAV I/O and synthesis
 via hound: sine, square, sawtooth, white noise, fades; playback and
 recording through termux-media-player and termux-microphone-record), and
 NoSQL storage (embedded ACID key-value database via sled with named
 sub-buckets and compare-and-swap, plus a blocking Redis client with
 strings, lists, hashes, TTL and raw commands), and a pure-Rust HTTP/1.1
 web server (tiny_http) with a radix-tree URL router (matchit, the same
 router axum uses under the hood) supporting named and catch-all path
 parameters, JSON/HTML/bytes responses and RFC 6455 WebSocket upgrades,
 and pure-Rust SVG charts via plotters (line, multi-line, bar, scatter
 and histogram outputs to standalone .svg files that any viewer can
 render without shipping a font), and HuggingFace text tokenizers
 (BPE / WordPiece / Unigram) via the official `tokenizers` crate in a
 pure-Rust configuration — reads any HF `tokenizer.json` and exposes
 encode / encode_batch / decode / vocab_size / token_to_id lookups,
 and on-device ONNX inference via `tract-onnx` (Sonos' production
 inference engine, 100% Rust, no CUDA / cuDNN / BLAS / ONNX Runtime
 C++), able to load .onnx models, inspect their input/output shapes
 and run f32 or i64 tensor inputs entirely on the phone's CPU, plus
 multi-input BERT-family loaders (input_ids + attention_mask, with
 or without token_type_ids) so DistilBERT / MiniLM / classic BERT
 classifiers and embedding models can be tokenized with std::tokenize
 and forwarded through std::onnx in a single pipeline on the device,
 and Wi-Fi introspection (std::wifi::scan / connection_info / set_enabled
 / signal_bars) via the official termux-wifi-* CLIs shipped by the
 Termux:API package, plus sentence-transformer style embedding
 pooling (std::onnx::run_bert_pooled — attention-mask-weighted mean
 pool of the encoder's last_hidden_state) and pure-Rust vector math
 (std::vector::dot / norm / cosine_similarity / normalize / add / sub
 / scale / argmax) for on-device semantic search over MiniLM
 embeddings, and PDF generation (std::pdf::new / add_page / add_text /
 set_color / add_line / add_rect / save) built on printpdf 0.7 in a
 no-defaults pure-Rust config (no azul-layout, no rust-fontconfig, no
 HTML rendering — just the core PDF writer with the 14 built-in fonts).

Package: zett
Version: 0.25.0
Architecture: arm
Maintainer: Alex Sanders Soto
Filename: ./zett_0.25.0_arm.deb
Size: 9637372
MD5sum: 38c2d442e8dfb9b9fc3a83cdf12df5fc
SHA1: 52ff0311b4f976fe692c30531125b276a01a13e5
SHA256: 7baed9b2f302b5db18f84fa72a1d572204b9d4efe2356be7616afb30e2564cdf
Description: TITAN compiler: HTTPS, crypto, Android, TUI, images, QR, system, audio, NoSQL, web server, charts, HF tokenizers, ONNX inference (BERT-family multi-input, sentence-transformer pooling), Wi-Fi scanning, vector math (semantic search), PDF generation
 Zett installs the TITAN Language Compiler, examples and documentation.
 Includes a modern standard library with real (non-simulated) modules:
 regex, uuid, hash (SHA/BLAKE3/HMAC), random, datetime, url, dirs,
 compress (gzip/zstd), archive (tar/zip), yaml, xml, blocking HTTPS
 client, DNS resolver, SMTP email, ChaCha20-Poly1305 & AES-GCM AEAD,
 Argon2id/bcrypt password hashing, JWT signing/verification, direct
 access to Termux:API for Android hardware/OS integration (battery,
 GPS, sensors, camera, SMS, clipboard, vibrate, notify, TTS),
 terminal/TUI helpers (colors, cursor, raw mode, keyboard events,
 readline with history, animated progress bars and spinners), image
 processing (PNG/JPEG/WebP/BMP/GIF: load, save, encode, resize, crop,
 rotate, grayscale, blur, brighten), QR code generation
 (ASCII/Unicode/SVG/PNG at every error-correction level), system
 tools (procfs CPU/memory/processes/disks/networks, file-system watcher
 via inotify, Unix signal handling), and audio (WAV I/O and synthesis
 via hound: sine, square, sawtooth, white noise, fades; playback and
 recording through termux-media-player and termux-microphone-record), and
 NoSQL storage (embedded ACID key-value database via sled with named
 sub-buckets and compare-and-swap, plus a blocking Redis client with
 strings, lists, hashes, TTL and raw commands), and a pure-Rust HTTP/1.1
 web server (tiny_http) with a radix-tree URL router (matchit, the same
 router axum uses under the hood) supporting named and catch-all path
 parameters, JSON/HTML/bytes responses and RFC 6455 WebSocket upgrades,
 and pure-Rust SVG charts via plotters (line, multi-line, bar, scatter
 and histogram outputs to standalone .svg files that any viewer can
 render without shipping a font), and HuggingFace text tokenizers
 (BPE / WordPiece / Unigram) via the official `tokenizers` crate in a
 pure-Rust configuration — reads any HF `tokenizer.json` and exposes
 encode / encode_batch / decode / vocab_size / token_to_id lookups,
 and on-device ONNX inference via `tract-onnx` (Sonos' production
 inference engine, 100% Rust, no CUDA / cuDNN / BLAS / ONNX Runtime
 C++), able to load .onnx models, inspect their input/output shapes
 and run f32 or i64 tensor inputs entirely on the phone's CPU, plus
 multi-input BERT-family loaders (input_ids + attention_mask, with
 or without token_type_ids) so DistilBERT / MiniLM / classic BERT
 classifiers and embedding models can be tokenized with std::tokenize
 and forwarded through std::onnx in a single pipeline on the device,
 and Wi-Fi introspection (std::wifi::scan / connection_info / set_enabled
 / signal_bars) via the official termux-wifi-* CLIs shipped by the
 Termux:API package, plus sentence-transformer style embedding
 pooling (std::onnx::run_bert_pooled — attention-mask-weighted mean
 pool of the encoder's last_hidden_state) and pure-Rust vector math
 (std::vector::dot / norm / cosine_similarity / normalize / add / sub
 / scale / argmax) for on-device semantic search over MiniLM
 embeddings, and PDF generation (std::pdf::new / add_page / add_text /
 set_color / add_line / add_rect / save) built on printpdf 0.7 in a
 no-defaults pure-Rust config (no azul-layout, no rust-fontconfig, no
 HTML rendering — just the core PDF writer with the 14 built-in fonts).

Package: zett
Version: 0.3.0
Architecture: arm
Maintainer: Alex Sanders Soto
Filename: ./zett_0.3.0_arm.deb
Size: 4991372
MD5sum: b38aef55c3e3155021222319dd952178
SHA1: 43ffcaf71f94bfe66b6de18e8e6e8a3dbb0d2bed
SHA256: a9f2958dc5ef8887d753e86b660928be8ac82fb5c589c82d8640affff199ec8d
Description: TITAN language compiler with real HTTPS, crypto and Android bindings
 Zett installs the TITAN Language Compiler, examples and documentation.
 Includes a modern standard library with real (non-simulated) modules:
 regex, uuid, hash (SHA/BLAKE3/HMAC), random, datetime, url, dirs,
 compress (gzip/zstd), archive (tar/zip), yaml, xml, blocking HTTPS
 client, DNS resolver, SMTP email, ChaCha20-Poly1305 & AES-GCM AEAD,
 Argon2id/bcrypt password hashing, JWT signing/verification, and
 direct access to Termux:API for Android hardware/OS integration
 (battery, GPS, sensors, camera, SMS, clipboard, vibrate, notify, TTS).

Package: zett
Version: 0.4.0
Architecture: arm
Maintainer: Alex Sanders Soto
Filename: ./zett_0.4.0_arm.deb
Size: 5153156
MD5sum: 1c4bf06ff96bc80c3eeee4c9452881f2
SHA1: e7da346478296e915dcdad3d440a595c553e28e0
SHA256: a35030c604804e917c658ba43821091eeba879052cd0d80ada11ca05c8d027be
Description: TITAN language compiler with HTTPS, crypto, Android and TUI
 Zett installs the TITAN Language Compiler, examples and documentation.
 Includes a modern standard library with real (non-simulated) modules:
 regex, uuid, hash (SHA/BLAKE3/HMAC), random, datetime, url, dirs,
 compress (gzip/zstd), archive (tar/zip), yaml, xml, blocking HTTPS
 client, DNS resolver, SMTP email, ChaCha20-Poly1305 & AES-GCM AEAD,
 Argon2id/bcrypt password hashing, JWT signing/verification, direct
 access to Termux:API for Android hardware/OS integration (battery,
 GPS, sensors, camera, SMS, clipboard, vibrate, notify, TTS), and
 terminal/TUI helpers (colors, cursor, raw mode, keyboard events,
 readline with history, animated progress bars and spinners).

Package: zett
Version: 0.5.0
Architecture: arm
Maintainer: Alex Sanders Soto
Filename: ./zett_0.5.0_arm.deb
Size: 5639504
MD5sum: 5cc1711365e51fe363f53f79df94e13b
SHA1: 888a358c7b3143fbb5c8a197a1207e1fd6489f36
SHA256: a0611d810e47a428c4a9ffd755cc746fe5da5c40a3864b8ffcfa45ef0bc52505
Description: TITAN language compiler with HTTPS, crypto, Android, TUI, images and QR
 Zett installs the TITAN Language Compiler, examples and documentation.
 Includes a modern standard library with real (non-simulated) modules:
 regex, uuid, hash (SHA/BLAKE3/HMAC), random, datetime, url, dirs,
 compress (gzip/zstd), archive (tar/zip), yaml, xml, blocking HTTPS
 client, DNS resolver, SMTP email, ChaCha20-Poly1305 & AES-GCM AEAD,
 Argon2id/bcrypt password hashing, JWT signing/verification, direct
 access to Termux:API for Android hardware/OS integration (battery,
 GPS, sensors, camera, SMS, clipboard, vibrate, notify, TTS), and
 terminal/TUI helpers (colors, cursor, raw mode, keyboard events,
 readline with history, animated progress bars and spinners), plus
 image processing (PNG/JPEG/WebP/BMP/GIF: load, save, encode, resize,
 crop, rotate, grayscale, blur, brighten) and QR code generation
 (ASCII/Unicode/SVG/PNG at every error-correction level).

Package: zett
Version: 0.6.0
Architecture: arm
Maintainer: Alex Sanders Soto
Filename: ./zett_0.6.0_arm.deb
Size: 5761168
MD5sum: 721f284c3a8070d04100b3cd26c13fba
SHA1: 39bc0721f1b1cb5e961501ae950f09fc51dc6a2a
SHA256: a0ec7b9a7790e7c16c11f5376cc002ef60051ef13f4aa085e7f2c24cbff30e16
Description: TITAN language compiler: HTTPS, crypto, Android, TUI, images, QR, system info
 Zett installs the TITAN Language Compiler, examples and documentation.
 Includes a modern standard library with real (non-simulated) modules:
 regex, uuid, hash (SHA/BLAKE3/HMAC), random, datetime, url, dirs,
 compress (gzip/zstd), archive (tar/zip), yaml, xml, blocking HTTPS
 client, DNS resolver, SMTP email, ChaCha20-Poly1305 & AES-GCM AEAD,
 Argon2id/bcrypt password hashing, JWT signing/verification, direct
 access to Termux:API for Android hardware/OS integration (battery,
 GPS, sensors, camera, SMS, clipboard, vibrate, notify, TTS),
 terminal/TUI helpers (colors, cursor, raw mode, keyboard events,
 readline with history, animated progress bars and spinners), image
 processing (PNG/JPEG/WebP/BMP/GIF: load, save, encode, resize, crop,
 rotate, grayscale, blur, brighten), QR code generation
 (ASCII/Unicode/SVG/PNG at every error-correction level), and system
 tools: procfs (CPU, memory, load average, processes, disks, networks),
 file-system watcher (inotify) and Unix signal handling.

Package: zett
Version: 0.7.0
Architecture: arm
Maintainer: Alex Sanders Soto
Filename: ./zett_0.7.0_arm.deb
Size: 5788268
MD5sum: c58d783d98a8afe61649a4c005ce7507
SHA1: f320d6cef22056a973a3e49dbb1cde341fd56f91
SHA256: 37930f1acc2bbbd1a777b186ef17b4c02334816c91ceb5b14e4402f86695518a
Description: TITAN compiler: HTTPS, crypto, Android, TUI, images, QR, system, audio
 Zett installs the TITAN Language Compiler, examples and documentation.
 Includes a modern standard library with real (non-simulated) modules:
 regex, uuid, hash (SHA/BLAKE3/HMAC), random, datetime, url, dirs,
 compress (gzip/zstd), archive (tar/zip), yaml, xml, blocking HTTPS
 client, DNS resolver, SMTP email, ChaCha20-Poly1305 & AES-GCM AEAD,
 Argon2id/bcrypt password hashing, JWT signing/verification, direct
 access to Termux:API for Android hardware/OS integration (battery,
 GPS, sensors, camera, SMS, clipboard, vibrate, notify, TTS),
 terminal/TUI helpers (colors, cursor, raw mode, keyboard events,
 readline with history, animated progress bars and spinners), image
 processing (PNG/JPEG/WebP/BMP/GIF: load, save, encode, resize, crop,
 rotate, grayscale, blur, brighten), QR code generation
 (ASCII/Unicode/SVG/PNG at every error-correction level), system
 tools (procfs CPU/memory/processes/disks/networks, file-system watcher
 via inotify, Unix signal handling), and audio (WAV I/O and synthesis
 via hound: sine, square, sawtooth, white noise, fades; playback and
 recording through termux-media-player and termux-microphone-record).

Package: zett
Version: 0.8.0
Architecture: arm
Maintainer: Alex Sanders Soto
Filename: ./zett_0.8.0_arm.deb
Size: 6113136
MD5sum: cedca2c9e8ed60362fd616f6b5f6c732
SHA1: 96814ee6b1615a375766545914788bcef0ef8811
SHA256: 59c7dbd9aff257527fe75218ada68b998a21fd482a79a1a6d28c9df65a8f6601
Description: TITAN compiler: HTTPS, crypto, Android, TUI, images, QR, system, audio, NoSQL
 Zett installs the TITAN Language Compiler, examples and documentation.
 Includes a modern standard library with real (non-simulated) modules:
 regex, uuid, hash (SHA/BLAKE3/HMAC), random, datetime, url, dirs,
 compress (gzip/zstd), archive (tar/zip), yaml, xml, blocking HTTPS
 client, DNS resolver, SMTP email, ChaCha20-Poly1305 & AES-GCM AEAD,
 Argon2id/bcrypt password hashing, JWT signing/verification, direct
 access to Termux:API for Android hardware/OS integration (battery,
 GPS, sensors, camera, SMS, clipboard, vibrate, notify, TTS),
 terminal/TUI helpers (colors, cursor, raw mode, keyboard events,
 readline with history, animated progress bars and spinners), image
 processing (PNG/JPEG/WebP/BMP/GIF: load, save, encode, resize, crop,
 rotate, grayscale, blur, brighten), QR code generation
 (ASCII/Unicode/SVG/PNG at every error-correction level), system
 tools (procfs CPU/memory/processes/disks/networks, file-system watcher
 via inotify, Unix signal handling), and audio (WAV I/O and synthesis
 via hound: sine, square, sawtooth, white noise, fades; playback and
 recording through termux-media-player and termux-microphone-record), and
 NoSQL storage (embedded ACID key-value database via sled with named
 sub-buckets and compare-and-swap, plus a blocking Redis client with
 strings, lists, hashes, TTL and raw commands).

Package: zett
Version: 0.9.0
Architecture: arm
Maintainer: Alex Sanders Soto
Filename: ./zett_0.9.0_arm.deb
Size: 6222180
MD5sum: fd12d64d8855cfe5a4f6fb31361900bb
SHA1: 1b10f7cc48f105d75cf8f8f1cc31ca93b1316e84
SHA256: 557e254da00d8f6a99bf8d4f8da26dac5aa2d063eeaa7a7ab3e41c8bc7dcb8bf
Description: TITAN compiler: HTTPS, crypto, Android, TUI, images, QR, system, audio, NoSQL, web server
 Zett installs the TITAN Language Compiler, examples and documentation.
 Includes a modern standard library with real (non-simulated) modules:
 regex, uuid, hash (SHA/BLAKE3/HMAC), random, datetime, url, dirs,
 compress (gzip/zstd), archive (tar/zip), yaml, xml, blocking HTTPS
 client, DNS resolver, SMTP email, ChaCha20-Poly1305 & AES-GCM AEAD,
 Argon2id/bcrypt password hashing, JWT signing/verification, direct
 access to Termux:API for Android hardware/OS integration (battery,
 GPS, sensors, camera, SMS, clipboard, vibrate, notify, TTS),
 terminal/TUI helpers (colors, cursor, raw mode, keyboard events,
 readline with history, animated progress bars and spinners), image
 processing (PNG/JPEG/WebP/BMP/GIF: load, save, encode, resize, crop,
 rotate, grayscale, blur, brighten), QR code generation
 (ASCII/Unicode/SVG/PNG at every error-correction level), system
 tools (procfs CPU/memory/processes/disks/networks, file-system watcher
 via inotify, Unix signal handling), and audio (WAV I/O and synthesis
 via hound: sine, square, sawtooth, white noise, fades; playback and
 recording through termux-media-player and termux-microphone-record), and
 NoSQL storage (embedded ACID key-value database via sled with named
 sub-buckets and compare-and-swap, plus a blocking Redis client with
 strings, lists, hashes, TTL and raw commands), and a pure-Rust HTTP/1.1
 web server (tiny_http) with a radix-tree URL router (matchit, the same
 router axum uses under the hood) supporting named and catch-all path
 parameters, JSON/HTML/bytes responses and RFC 6455 WebSocket upgrades.

