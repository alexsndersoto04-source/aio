name: Moon checks
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
jobs:
  titan-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # Fase 42A: cpal necesita ALSA para compilar en Linux
      - name: Instalar ALSA (audio Linux)
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends libasound2-dev
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - name: Compilar el backend Titan (zett check)
        run: cargo run --quiet -p titan_cli -- check projects/moon/src/main.titan
  frontend-build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - run: npm ci
      - run: npm run build
